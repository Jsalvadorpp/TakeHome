# Copy this file to .env and fill in your values.
#
#   cp .env.example .env
#
# .env is gitignored — never commit real credentials.
# This file (.env.example) is safe to commit because it has no real values.

# ── Postgres credentials ────────────────────────────────────────────────────
# Used by docker-compose to initialize and connect to the database container.
POSTGRES_USER=postgres
POSTGRES_PASSWORD=changeme
POSTGRES_DB=postgres

# ── Application database URL ─────────────────────────────────────────────────
# Used when running Python OUTSIDE Docker (e.g. local development without containers).
# Inside Docker this value is ignored — docker-compose builds the URL automatically
# using the credentials above and the internal "db" hostname.
#
# For production (e.g. AWS RDS, Cloud SQL), set this to your managed DB URL:
#   DATABASE_URL=postgresql://user:password@your-rds-host.amazonaws.com:5432/hail_db
DATABASE_URL=postgresql://postgres:changeme@localhost:5432/postgres
